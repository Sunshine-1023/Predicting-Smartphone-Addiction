data:
  directory: data/raw
  processed_directory: data/processed
  sample_rows: null
artifacts:
  directory: artifacts/runs
runtime:
  threads: 4
cv:
  n_splits: 5
  seeds: [42]
features:
  groups: [raw]
  exclude_columns: []
  masking:
    enabled: false
    fraction: 0.20
tuning:
  sample_fraction: 0.5
  n_trials: 20
  n_candidates: 3
model:
  name: catboost
  params: {}
